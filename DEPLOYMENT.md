# 公网访问说明

## 推荐方案：部署到云端

将项目部署到 Vercel、Netlify 或 Cloudflare Pages 后，会得到一个公网 HTTPS 地址。任何地点、任何网络的电脑都可以直接访问这个地址：

```text
https://你的项目地址.example
```

部署后的访问关系是：

```text
公司电脑 ─┐
          ├─ 公网网址 ─ 云端网站
家里电脑 ─┘
```

两台电脑不需要在同一个局域网，也不需要启动公司电脑上的开发服务器。

## 本地开发方式

```bash
npm run dev
```

这只适合开发者在当前电脑上测试。`localhost` 永远指向“正在访问它的那台电脑”。

## 数据同步注意事项

当前 MVP 使用浏览器 `localStorage` 保存项目，因此：

- 同一个网址可以被不同电脑打开；
- 但不同电脑默认不会共享项目数据；
- 当前版本可以导出 JSON 项目包，并在另一台电脑点击“导入项目”继续工作；
- 要实现真正的跨设备同步，需要登录系统、后端 API 和云端数据库。

## 不推荐的方式

直接在公司电脑上开放端口、配置路由器端口转发，或者使用临时隧道，可以临时访问，但稳定性、安全性和权限管理都不适合正式使用。
