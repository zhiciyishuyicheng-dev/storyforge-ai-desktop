# StoryForge · AI 短剧工坊

一个从剧本输入开始，按步骤管理短剧生产流程的 Windows 本地 PC 应用。

## 安装 PC 版

打包完成后，在 `release-v0.10.1` 文件夹中使用：

- `StoryForge-Setup-0.10.1-x64.exe`：Windows 安装程序。

安装时可以选择安装位置，并创建桌面快捷方式。

## 生成 PC 安装包

```bash
npm install
npm run desktop:pack
```

生成文件保存在 `release-v0.10.1` 文件夹。

## 本地启动

```bash
npm install
npm run dev
```

打开终端输出的本地网址即可使用。

## 发布到公网（适合公司电脑 + 家里电脑）

不要让家里电脑访问公司电脑的 `localhost`，也不要依赖公司局域网 IP。项目已经包含 Vercel 和 Netlify 的静态部署配置：

1. 将整个项目上传到 GitHub。
2. 在 Vercel 或 Netlify 中导入该仓库。
3. 构建命令使用 `npm run build`，发布目录使用 `dist`。
4. 发布后得到一个所有电脑都能访问的网址。

也可以在安装并登录 Vercel CLI 后运行：

```bash
npx vercel
```

发布后，公司电脑和家里电脑都访问同一个 `https://...` 网址，不要求两台电脑在同一网络，也不要求公司电脑一直开机。

## 当前能力

- 粘贴剧本或导入 TXT / Markdown 文件
- 一键载入示例剧本
- 按故事分析、分集大纲、短剧剧本、分镜脚本、视觉素材、视频生成推进
- 每一步展示生成状态、结果预览、重新生成和进入下一步
- 浏览器本地自动保存项目状态（当前每台电脑各自保存一份）
- 复制当前结果、导出 JSON 项目包
- 导入 JSON 项目包，可在另一台电脑继续工作
- 按 Seedance 2.0 Skill 生成逐镜头提示词，并根据剧情为每镜动态分配时长
- 自动分配 `@Image`、`@Video`、`@Audio` 的引用用途并检查素材限制
- 支持复制单镜头或全部 Seedance 提示词
- 故事分析到分镜脚本由 DeepSeek 自动连续生成，无需逐步确认
- 第五步暂时关闭 Seedream 5.0 与 GPT Image 2 模型生图，统一改为上传本地参考图片
- 为每个人物、场景、群像和道具分别提供独立上传入口，可为同一项追加多张参考图
- 在全部素材卡片末尾提供“＋ 添加遗漏资源”，可自定义补充主持人、功能人物、额外场景或遗漏道具
- 自定义资源支持选择类型、填写名称、立即上传图片和删除资源项
- 上传图片自动复制到“文档/StoryForge/项目名称/项目编号/视觉素材/本地上传”目录
- 每次上传参考图后保持页面原有滚动位置，不再跳回页面顶部
- 支持预览、在资源管理器中定位、清空单项关联，并在重启软件后继续使用
- 所有必需素材上传完成后，手动确认并进入第六步
- 第六步接入火山方舟 Seedance 2.0 和 Seedance 2.0 Fast，按分镜逐条创建真实视频任务
- 按火山方舟官方多素材规则，将 `@ImageN` 严格绑定为 Seedance 请求中的第 N 张参考图，并以 Base64 参考素材提交
- 第六步逐镜展示“图片编号、准确素材名称、用途、缩略图和实际文件”，同一素材上传多张图时可明确选择本镜头使用哪一张
- 第六步每个镜头支持“引用第五步已有图”和“上传并添加新图”，无需退回第五步即可补充人物、场景、群像或道具参考图
- 新增参考图会自动创建下一个连续 `@Image` 编号、写入当前镜头提示词并完成素材绑定，单镜头最多九张
- 第六步新增的参考图支持删除，删除后会同步清理提示词、上传计划和绑定，并自动整理后续编号
- 第六步所有参考图节点均支持“删除引用”；删除只影响当前镜头，不删除第五步原图，也不影响其他镜头
- 删除触发真人验证的节点时，会取消当前镜头等待中的认证状态，并确保该图片不再进入 Seedance 请求
- 兼容没有结构化上传计划的旧项目：自动从现有提示词恢复 `@Image1～N`，可在下拉框中从第五步全部图片里手动绑定
- 第六步绑定区域使用紧凑布局并限制视频列宽，提示词和绑定面板不再覆盖左侧视频播放器
- 人物、场景、群像和道具只按第五步素材的完整名称精确匹配，不再使用模糊名称、素材类型或列表位置兜底
- 发送前校验编号必须从 `@Image1` 连续排列、提示词引用必须与上传计划一致、素材名称必须唯一且图片必须存在；发现错号、跳号或缺图时阻止生成
- Seedance 请求保持参考图原始顺序，不去重、不重新编号；提交提示词会自动附加逐项素材对应关系和禁止人物/场景互换约束
- 支持 9:16、16:9 等比例、480p / 720p、4 至 15 秒和同步音频开关
- 第六步优先读取每个分镜的结构化时长，旧项目可从“生成九秒”或“0-12 秒”类提示词自动恢复秒数
- 视频设置中的镜头时长为“按分镜自动”，不再显示容易被误解为统一时长的秒数下拉框
- 第六步同时提供“生成下一个镜头”和“批量生成全部视频”按钮，每张镜头卡也可单独生成或重试
- 第六步镜头卡改为每行一个，并完整换行展示对应视频提示词，不再只显示前 120 个字符
- 第六步每个镜头的视频提示词支持直接编辑、自动保存和恢复原始提示词，单镜与批量生成均优先使用编辑稿
- Seedance 因参考图可能包含真人而拦截时，自动定位具体素材并打开真人素材认证恢复窗口
- 支持为参考图绑定火山方舟可信素材 Asset ID，改用 `asset://` 提交后自动重试当前镜头并继续原批量任务
- 演员本人仍须在火山方舟完成扫码、人脸核验和肖像授权，程序不会绕过真人认证
- 视频任务支持排队、生成、下载、完成和失败状态，失败镜头可单独重试
- 生成视频自动保存到“文档/StoryForge/项目名称/项目编号/视频片段”目录
- Seedance API Key 使用 Windows 本机加密保存
- 明确显示模型未开通、Key 无效、额度不足、请求超时等真实接口错误
- 已移除九宫格制作参考板，不再生成或展示九宫格图片任务
- 自动流程在视频生成前停止，视频必须手动点击开始
- 使用 Codex 风格的 Windows 系统字体与清晰字号
- 支持 `Ctrl +`、`Ctrl -` 和 `Ctrl 0` 调整界面缩放
- 从人物表和对白中识别真实人物姓名，排除标题、类型、规格等元数据
- 除即梦平台要求的素材引用标记外，分镜提示词全部使用中文
- 视频生成前四个文本阶段调用 DeepSeek V4 Pro，不再使用本地模板
- DeepSeek API Key 使用 Windows 本机加密保存
- DeepSeek 返回内容经过人物、场次、分镜完整性和中英文混排校验
- DeepSeek 生成分镜时强制 `uploads.asset` 与第五步素材名称逐字一致，并在每条提示词中点名人物、场景及其 `@Image` 编号
- 人物素材改为独立单人参考图，场景素材改为单一机位参考图，不再要求容易被视频模型误判为多人或多场景的三视图、多画格拼版

视频生成前四个文本阶段由 DeepSeek V4 Pro 完成，第五步按人物、场景、群像和道具上传本地参考图，第六步由火山方舟 Seedance 2.0 逐镜或批量生成视频。接口失败时会显示真实错误，不会退回本地演示模板。

## 更新记录

### 0.11.5

- 修复删除中间参考图后可能残留 `@Image7` 跳号、提示词仍保留 `@Image6` 的问题
- 删除后不再简单执行“后续编号减一”，改为按当前剩余节点顺序从 `@Image1` 完整重新编号
- 上传计划、提示词引用和本地文件绑定使用同一份旧编号到新编号映射，避免三者错位
- 打开历史错误项目时自动修复跳号、无对应图片的残留引用和错误绑定，无需用户再次删除
- 新增 `1、2、3、4、5、7` 自动修复为 `1～6` 的回归测试，严格绑定测试增加至九项

### 0.11.4

- 第六步所有原始和新增 `@Image` 节点增加“删除引用”按钮
- 删除节点后从当前镜头 Seedance 请求中移除该图片，磁盘文件和第五步素材保留
- 删除任意中间节点时，后续上传计划、提示词引用和本地绑定自动连续前移
- 提示词中被删除的原始引用替换为普通中文素材名称，保留剧情语义但不再发送参考图
- 删除当前镜头触发真人验证的节点时，自动取消该镜头待认证状态
- 新增任意节点删除与连续重编号测试，严格绑定测试增加至八项

### 0.11.3

- 修复旧项目点击“引用第五步已有图”后提示“当前镜头无法添加参考图”的问题
- 当旧项目完全缺少 `storyboard.shots` 数组时，自动恢复并创建当前镜头数据后再添加素材
- 修复旧项目无法准确解析素材名称时，绑定标题可能错误显示为“@”的问题
- 新增“完全缺少镜头数组仍可添加素材”的回归测试，严格绑定测试增加至七项

### 0.11.2

- 第六步“本镜头素材硬绑定”增加“引用第五步已有图”入口
- 增加“上传并添加新图”入口，可直接创建人物、场景、群像、道具或其他素材并选择本地图片
- 新图上传完成后自动添加到当前镜头、创建连续 `@Image` 编号并写入提示词
- 根据素材类型自动生成明确用途，人物、场景、群像和道具使用不同的一致性约束
- 新增素材可从当前镜头删除，后续引用编号、提示词和保存的绑定同步前移
- 恢复原始提示词时自动清理第六步额外添加的引用，避免残留上传计划

### 0.11.1

- 修复旧项目缺少结构化 `uploads` 时显示“没有有效上传计划”且无法编辑的问题
- 自动从旧提示词恢复 `@Image1～N`，能识别准确素材名称时自动绑定对应图片
- 无法自动识别时，为每个编号提供第五步全部已上传图片的可编辑下拉框
- 下拉选项同时显示素材名称和文件名，明确说明应在何处完成绑定
- 修复绑定面板最小宽度过大导致右侧内容覆盖视频播放器的问题
- 新增旧项目自动恢复和手动绑定测试，严格绑定测试增加至六项

### 0.11.0

- 按火山方舟官方文档重构 Seedance 多素材引用链路，`@ImageN` 与请求中第 N 张图片严格一一对应
- 第六步新增逐镜头素材硬绑定面板，完整显示编号、人物或场景名称、用途、缩略图和实际本地文件
- 同一人物或场景上传多张参考图时，可为每个镜头手动选择唯一使用图，选择结果自动保存
- 删除素材名称模糊匹配、素材类型兜底、列表位置兜底、同一素材多图自动展开、请求图片去重和编号重排
- 增加发送前完整性校验：连续编号、引用集合、精确素材名、唯一素材名、九张上限和本地文件状态
- 发送给 Seedance 的提示词自动添加“图片编号—准确素材—唯一用途”映射及人物和场景禁止互换约束
- DeepSeek 分镜输出规则改为准确点名每个角色与场景，并强制分镜上传计划和第五步素材清单逐字一致
- 主持人、助理、医生等出镜、说话或推动剧情的功能人物必须进入独立人物素材清单
- 第五步人物参考图改为独立单人图，场景参考图改为单一机位图，移除三视图和多画格场景拼版要求
- 新增多素材严格绑定自动测试，覆盖素材列表乱序、多图选择、引用缺失和禁止位置兜底

### 0.10.1

- 第六步增加逐镜头提示词编辑器，支持自动保存、恢复原始提示词和编辑后重新生成
- 第六步视频镜头卡改为单列展示，完整显示每个镜头的全部提示词
- 增加 Seedance 真人素材拦截识别、可信素材 Asset ID 绑定及认证后自动续跑
- 上传参考图完成后保持主内容区、侧边栏和项目列表的原有滚动位置
- 在第五步全部素材卡片末尾增加“＋ 添加遗漏资源”入口
- 支持自定义人物、场景、群像、道具和其他参考资源
- 创建自定义资源后立即打开本地图片选择窗口
- 自定义资源参与第五步完整性检查和第六步按名称匹配
- 支持删除误加的自定义资源，磁盘中的原图片不会被误删

### 0.10.0

- 暂时关闭 Seedream 5.0 与 GPT Image 2 模型生图入口，保留底层代码供后续恢复
- 第五步改为按人物、场景、群像和道具分别上传本地参考图
- 上传图片复制到当前项目素材目录，支持多图追加、预览、定位和清空关联
- 第五步全部素材确认完成后才进入第六步
- 第六步明确提供逐个生成、批量生成和单镜头重试入口
- Seedance 根据每个镜头的素材名称匹配本地参考图，不再只按全局顺序引用

### 0.9.3

- 移除第六步设置中的统一秒数下拉框，改为只读的“按分镜自动（4-15 秒）”
- 保存视频模型、比例、分辨率或生成方式时，不再覆盖每个分镜自身的时长

### 0.9.2

- 第六步增加“批量连续 / 逐镜确认”生成方式开关
- 逐镜确认模式一次只提交一个 Seedance 任务，下载完成后等待用户确认下一镜
- 支持逐镜进度恢复、当前镜头失败重试和从头逐镜重新生成

### 0.9.1

- 第六步改为逐镜读取分镜时长，分别向 Seedance 提交 4 至 15 秒任务
- 兼容中文秒数、明确总时长和分段时间轴，超出模型范围时自动校正
- 第六步任务卡和侧边状态显示各镜头实际生成秒数

### 0.9.0

- 第六步接入火山方舟 Seedance 2.0 与 Seedance 2.0 Fast
- 增加视频模型、比例、分辨率、时长、同步音频和 API Key 设置
- 使用第五步本地参考图生成分镜视频，并自动重写素材引用编号
- 增加异步任务轮询、逐镜进度、失败重试、MP4 下载和本地目录定位

### 0.8.0

- 第五步增加 OpenAI GPT Image 2 生图渠道
- 支持 GPT Image 2 尺寸、质量、API Key、批量生成和失败重试
- 将制作流程和剧本入口固定到左侧，并提高界面字体可读性

### 0.7.1

- 接入火山方舟 Seedream 5.0，自动完成第五步真实参考图生成
- 增加 Seedream 模型、清晰度、API Key 和连接测试设置
- 增加图片批量生成、失败重试、本地保存和目录定位能力
- 充值但未开通模型时，显示明确的开通提示和模型名称
- 删除九宫格制作参考板及旧项目中遗留的九宫格任务
- 将 DeepSeek 自动流程调整为前四步，第五步交由 Seedream 执行

### 0.6.0

- 接入 DeepSeek V4 Pro，生成故事分析、分集大纲、短剧剧本和分镜脚本
- 根据分镜 Skill 输出中文逐镜头提示词及视觉素材提示词
- 增加多项目管理、项目导入导出和本机自动保存

## README 维护约定

每次新增、修改或删除产品能力时，必须同时更新“当前能力”和“更新记录”；安装包版本、输出目录或使用方式发生变化时，也必须同步修改对应说明。

如果需要公司电脑和家里电脑看到同一批项目、剧本和生成结果，需要再增加登录、后端接口和云端数据库；仅靠浏览器本地保存不会自动同步。
